query AnimeMedia(
    $id: Int,
) {
    page: Page (
        page: 0,
        perPage: 1
    ) {
        media(
            id: $id
        ) {
            id
            coverImage {
                extraLarge
            }
            title {
                english
            }
            episodes
            averageScore
            description(
                asHtml: true
            )
            nextAiringEpisode {
                timeUntilAiring
            }
        }
    }
}